import { Module } from '@nestjs/common';

/**
 * Exam-cell endpoints (manage mark structures, marks entry across programmes)
 * land here. Each handler MUST follow the RBAC enforcement contract — see
 * nucleus-server/CLAUDE.md.
 */
@Module({})
export class ExamCellModule {}
